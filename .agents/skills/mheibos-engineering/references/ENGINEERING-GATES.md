# Gates de engenharia

Confirmar fontes/invariantes; domínio antes da interface; autorização antes da persistência; integridade histórica; migração/rollback; eventos, auditoria e idempotência. Executar testes aplicáveis (unidade, domínio, integração, autorização, migração e falhas), lint, type checking, checks de framework e inspeção do diff descobertos no projeto.

Na fotografia atual não existem código nem comandos executáveis do produto. Não inventar comandos; redescobrir quando evoluir. Para ENG, aplicar também `docs/ENG-QUALITY-GATES.md` e `validate_eng_series.py`.
